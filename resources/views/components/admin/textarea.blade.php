<div class="form-group {{ $class ?? 'mb-3' }}">
    <textarea class="{{ $class ?? '' }}" id="{{ $title }}" name="{{ $title }}" placeholder="{{ $placeholder ?? '' }}">{{ $value ?? '' }}</textarea>
</div>